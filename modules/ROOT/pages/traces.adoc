= Examining Traces
:page-notice-banner-message: Open Beta Release: Traces is in open beta. Any use of Anypoint Monitoring features in the beta state is subject to the applicable beta services terms and conditions.
:page-info-pills: anypoint-pricing-titanium
:entities: Agents, brokers, MCP servers, and Mule apps
:auto-refresh: every 5, 15, or 30 minutes, every hour, or one time per day

Use Traces to track the flow of requests that travel through service pipelines. Get data from the Open Telemetry (OTEL) spans that form your traces. Troubleshoot requests with errors or slow response times, optimize your resource allocation, and track calls end-to-end. Monitor the performance of a specific transaction as it travels through services. 

[[traces-overview]]
//don't change order of numbers in graphic or calloutlist
image::traces.png["Span list in Traces Overview"]

[calloutlist]
. Traces in Anypoint Monitoring
. <<environment, Environment selector>> for your deployments (such as Sandbox or Production)
. <<filters, Filter>> field for queries that narrow the list of logs to return
//{entities} = Agents, Brokers, MCP Servers, and Mule apps
. List of spans for the supported {entities}
. Lookback selector for the period of time to capture in the *Span List* (from the default of 15 minutes to the last 7 days)
. Auto-Refresh {auto-refresh}
. Refresh the page manually

In addition to reviewing traces in Anypoint Monitoring, you can use xref:telemetry-exporter.adoc[Telemetry Exporter] to export traces to third-party observability and analytics platforms.

=== Supported Traces

Traces include spans for:

* Agentic assets: Agent, broker, MCP server instances
* Flows: Mule flows for the selected task of the specific service
* Logs: Contextual logs for the selected task
* Events: Mule runtime events or user actions for the selected service

=== Hosts

Traces in Anypoint Monitoring displays spans from entities deployed to:

* CloudHub 2
* Runtime Fabric

//TODO: VERIFY with Bhavin 

=== Sampling Rate

Trace data sampling rate:

* 100%: Agents, brokers, and MCP servers
* 10%: Mule apps and APIs

//TODO: VERIFY ask Bhavin or Sathish

== Before You Begin

Before using traces, ensure that you have:

include::reuse::partial$billing/pricing.adoc[tag=prereqBulletTitanium]

* xref:access-management::permissions-by-product.adoc#monitoring[Permission to view] Mule apps, APIs, and instances of agents, brokers, and MCP servers in Anypoint Monitoring 
* For apps deployed to CloudHub 2:
** xref:cloudhub-2::ch2-deploy-shared-space.adoc#enable-trace-data-collection[Enabled trace data collection for your app in a shared space]
** xref:cloudhub-2::ch2-deploy-private-space.adoc#enable-trace-data-collection[Enabled trace data collection for your app in a private space]
* For Runtime Fabric, enabled trace data collection on apps deployed to xref:runtime-fabric::use-anypoint-monitoring.adoc#enable-trace-data[Runtime Fabric]
//* Requirements for Mule apps:
//TODO: MULE VERSION RELATES TO JUST MULE APPS OR MORE? AND VERIFY VERSION.
* Mule runtime version 4.6 or later
//TODO: CONNECTOR RELATES TO JUST MULE APPS OR MORE? AND VERIFY VERSION.
* HTTP Connector version 1.8 or later


[[trace-overview]]
== Get Trace Data from Spans

View a list of spans from across entities ({entities}) in *Traces Overview*. Get high-level details on each root span in a trace. Use filters to narrow the scope of spans in the *Spans List*. Get additional detail on each span in the hierarchy of spans that form a trace (from root span to its descendants). 

include::partial$include-nav-steps-no-permissions.adoc[]

include::partial$include-nav-distributed-tracing.adoc[]
. Make sure you're using the <<business-groups, business group>> that contains the spans.
. Select up to the last 7 days of spans (see <<traces-overview, item 5>>).
+
15 minutes is the default.
. Select the <<environments, environment>> that contains your spans.
. Optionally, provide a <<filters, filter query>> to narrow the search results.
. To get <<trace-details, trace details>> on a span in the *Span List*, click a span.
+
*Trace Details* displays the selected instance's traces in a span tree, starting from the root span for the instance.
+
image::trace-details.png["An instance's spans in the Traces Details page"]

.. *Trace Details* page for a span selected from *Trace Overview*
.. High-level data about the selected span:

* *Status Code*: The HTTP response status of the trace (for example `0 No Response`, `200 OK`,`500 Internal Server Error`)
* *Trace Start Time*: The time the trace started, in UTC
* *Total Response Time*: The time taken for the trace to execute 
* *Root Span*: The request or operation that initiated the trace
.. Search for spans by full or partial matches to the span name
+
The page displays matching spans. 
.. Trace hierarchy of spans, starting from the root span through its descendants
.. Trace ID of the root span
. To get more information about a span in the details page, click the span:
+
image::trace-details-span.png["Details of a span in the Traces Details page", width=90%]

.. *Span Name*: Name of the span.
.. Span details: 

* *Span Duration*: Time the span took to execute
//TODO: what does UNSET mean? Are there any other statuses?
* *Span Status*: Status of the span (*OK*, *ERROR*, *Unset*)
* *Span ID*: ID of the span
* *Span Kind*: Source of the span operation or request (*Server*, *Internal*, *Client*)
* *Span Start Time*: Time that the span started, in UTC
* *Trace ID*: The ID of the trace in which the span occurred
* *Correlation ID*: Unique identifier that's generated before sending an event to the next processor in the Mule flow (see xref:mule-runtime::correlation-id.adoc[])
* *HTTP Status Code*: HTTP status code of a span that is a request. Statuses include *0 No Response*, *200 OK*, and *500 Internal Server Error*.
* *Entity Type*: Type of entity associated with the trace. Types include *App*, *API*.
//TODO: BAD LINK TO ENTITY IN INSIGHTS, WILL WE FIX IT?
* *Entity Name*: The name of the entity associated with the trace
* <<business-groups, *Business Group*>> that the span belongs to
* <<environments, *Environment*>> that the span belongs to
* *Parent Span ID*: ID of a span that contains the selected span 
+
Parent spans don't have a value in this field. Use the Trace ID to track this span.
//TODO: BAD LINK TO LOGS IN MONITORING, WILL WE FIX IT?
* *View Logs*: Find the root span in Anypoint Monitoring logs. 

[[filters]]
== Filter Spans

To narrow the list of spans in *Traces Overview*, provide a filter query in the search field.  

. Place your cursor within the search field and click to display a list of filters. 
. Select a filter from the field's dropdown. 
+
[[filter-list]]
[%header,cols="1a,1a,3a"]
|===
|Filter |Operators |Description
| `api_instance_id` | `=` |Version of an API instance that's associated with a span. You can find the instance's ID in API Manager. 
| `correlation_id` | `=` |Correlation ID associated with a span (see xref:mule-runtime::correlation-id.adoc[]).
| `entity_name` | `=`, `IN` | Name of entities associated with spans. 
| `http_status_code` | `=`, `IN` | HTTP status codes associated with span. 
| `span_id` | `=` | ID of a span contained in traces.
| `span_name` | `=`, `IN` | Name of a span. 
| `trace_id` | `=`, `IN` | ID associated with a span.
|===
. Select an operator for the filter:

* `=` accepts a single alphanumeric value.
* `IN` accepts one or more comma-separated, alphanumeric values. The values get processed as logical ORs. 
. Select a value from the search field's *Top Value* list. If the value you're looking for isn't in the list, start typing a partial match to your value, and then select the value.
+
Tip: If you're providing search values to the `IN` operator (such as `entity_name IN ['value1', 'value2']`), manually adding a comma after a value before selecting another value from the *Top Value* list can help with valid query formation.
+
Example: 
+
This example gets spans named `my-span` that have status code '404', '502', or '200':
+
`http_status_code IN [ '404', '502', '200' ] span_name = my-span`

To edit or delete a filter, click the filter, and then click *Edit Filter* or *Delete Filter* from the *Actions* list for the filter. To avoid issues when editing, you can delete the filter and then add it back with the values you want.

Tips:

* If no traces appear after applying a query, change your filters, the lookback setting, or business group.
* Selecting multiple filters of the same type isn't allowed.
* Filters get processed as logical ANDs. If a span doesn't match one of the filters, the query won't return spans.

[[environments]]
== Environments

Select the environment containing the spans you want to review.

Your organization administrator xref:access-management::environments.adoc#to-create-a-new-environment[creates] the environments that are available to you.

Anypoint Platform supports these _types_ of xref:access-management::environments#types-of-environments.adoc[environments]:

* *Design*
* *Production*
* *Sandbox*

[[business-groups]]
== Business Groups

If you work from the root organization level, you can query all spans across the entire organization. If you're working from a business group that's a descendant of the root, the query returns spans from that business group only.

To view or select a different business group, use the business group dropdown.

image::business-groups.png["Business group menu"]

[calloutlist]
. Business group that contains spans in the *Span List*
. Selectable business groups
+
In the example, the root organization is active.


== See Also
* xref:telemetry-exporter.adoc[]
* xref:cloudhub-2::ch2-deploy-shared-space.adoc#enable-trace-data-collection[Enabled trace data collection for your app in a shared space]
* xref:cloudhub-2::ch2-deploy-private-space.adoc#enable-trace-data-collection[Enabled trace data collection for your app in a private space]
