= FAQ: Anypoint Monitoring Logs

Anypoint Monitoring logging aggregates log events so you can manage, search, filter, and analyze them.
Get answers to common questions about storage limits, deletion, log forwarding, and subscription changes.
For full documentation, see xref:index.adoc[Anypoint Monitoring].

== How do I get access to the Anypoint Monitoring logs?

The Anypoint Monitoring logging feature is available only with Titanium or Integration Advanced subscriptions.
For more information about Titanium subscription pricing, contact your account team.

== What are the storage limits for Anypoint Monitoring logs?

With a Titanium subscription, you receive 200 GB per production vCore and 50 GB per sandbox vCore.
Storage is shared globally across all apps and across both logs and metrics usage.
For example, if you have one production vCore, you have 200 GB of shared storage across all apps.

You also receive additional storage equal to 10% of your purchased storage, allocated for indexes of the most recent raw logs only.
Specifically, each production vCore includes 20 GB of searchable storage and each pre-production vCore includes 5 GB.
To calculate your total searchable storage allocation:

----
10% × (num_vcore_prod × 200 + num_vcore_preprod × 50)
----

Total searchable storage across the organization is capped at 6 TB regardless of the number of vCores purchased.

For details, see xref:performance-and-impact.adoc#titanium[Titanium Subscription Limits].

== Is there a maximum length for a single log line?

Yes. Each log line is truncated at 8 KB, including date and thread metadata.
To log longer segments of data, split them into chunks smaller than 8 KB each.

== What happens when the storage limit is reached?

When storage reaches capacity, the oldest logs are deleted first to reclaim space.

== Can I recover logs that have been deleted or overwritten?

No. After logs are deleted or overwritten, they cannot be recovered.

== What does the Anypoint Monitoring log feature do?

Anypoint Monitoring logging aggregates your log events so you can manage, search, filter, and analyze them.
For details, see xref:logs.adoc[Logs in Anypoint Monitoring].

== How are logs stored in Anypoint Monitoring?

Anypoint Monitoring stores raw logs and searchable logs separately.

* xref:performance-and-impact.adoc#titanium[Performance and Impact: Titanium]
* xref:raw-data.adoc[Raw Data in Anypoint Monitoring]

== Can I delete logs for specific applications and dates?

No. You cannot delete logs directly.
In cases where sensitive data may be exposed in logs, you can raise a support case with MuleSoft.
Requests to delete logs for other reasons are likely to be rejected.

== If I raise a support case to delete logs, what will MuleSoft delete?

The answer differs for raw logs and searchable logs.

*Raw logs* can be scoped by business group, environment, application, and date range (UTC).
MuleSoft can delete a targeted subset of raw logs without affecting other data.

*Searchable logs* cannot be scoped by business group, environment, or application.
Deletion is performed by dropping whole indices under the root organization.
Under the current multitenant Log Search architecture on Hyperforce, a single searchable-log index can span several days, weeks, or months.
The exact coverage depends on your SKU, index size, and storage usage.

As a result, the actual date range deleted will be equal to or larger than the range you request — in some cases by a significant margin.

Before executing the deletion, MuleSoft runs a dry run of the operation and shares the cumulative date range that will be dropped, along with the list of affected indices, so you can confirm before proceeding.

WARNING: Deleted logs cannot be recovered or re-imported. Review the dry-run results carefully before confirming.

== Can I import log data back into the system after deletion?

No. It's not possible to import logs back into the system after deletion.

== What happens to log data when I change my subscription?

Log data is lost and the storage that held it is terminated within an hour when you change subscriptions (for example, from Titanium to Platinum or from Platinum to Titanium).
The infrastructure changes and metrics are not migrated.

Log data is also lost when moving from a Titanium trial to a paid Titanium subscription, because trials run on a multitenant cluster that is separate from the dedicated single-tenant infrastructure used for paid Titanium.

IMPORTANT: Back up any logs you need before changing your subscription.

== How does Anypoint Monitoring connect to Runtime Fabric, and how many connections does it use?

Each Runtime Fabric app has a sidecar container that makes one persistent connection to Anypoint Monitoring to send logs.
Each pod uses one connection.

== What subscription does Runtime Fabric require for Anypoint Monitoring?

All deployment targets include a basic level of monitoring with the base subscription.
Runtime Fabric requires a Platinum subscription for general monitoring.
Certain features, such as logging, require a Titanium subscription.

For details, see xref:performance-and-impact.adoc[Understanding Performance Impact, Limits, and Data Retention].

== Why is my application not appearing in the Anypoint Monitoring log search filter?

This can happen when recent log lines are not available or when a large number of apps are reporting recent logs.
For more information, see xref:apps-not-showing-sidebar.adoc[Not All Apps Are Showing in the Left Sidebar in Anypoint Monitoring].

== If I disable Anypoint Monitoring for a CloudHub 1.0 application, will logs still be forwarded to Anypoint Platform?

Yes. If you disable Anypoint Monitoring by setting the following property:

[source,text]
----
anypoint.platform.config.analytics.agent.enabled=false
----

Logs are still forwarded to Anypoint Platform and remain visible in the Anypoint Monitoring logs UI.

== Can I disable Anypoint Monitoring log forwarding while keeping metrics collection enabled?

Yes. The steps vary by deployment target:

* *CloudHub 1.0*: See xref:cloudhub-disable-log-forwarding.adoc[How to Disable Log Forwarding to the Anypoint Monitoring Titanium Backend].
* *CloudHub 2.0*: Private space and shared space have separate procedures. See xref:ch2-disable-log-forwarding.adoc[Disable Log Forwarding in CloudHub 2.0].
* *Hybrid/on-premises*: See xref:hybrid-disable-log-forwarding.adoc[Disable Log Forwarding for Hybrid Deployments].

== Are Mule runtime logs (mule_ee.log) available in Anypoint Monitoring log search?

No. You cannot view on-premises `mule_ee.log` files directly through Anypoint Monitoring log search.

== What happens to logs after I delete an application?

Logs stored in Anypoint Monitoring remain accessible after an application is deleted.
You can retrieve them using the xref:archive-api.adoc[Anypoint Monitoring Archive API].
Logs are deleted when storage capacity is reached, starting with the oldest logs.

== Why is there a delay between the Anypoint Monitoring UI and the raw Archive API data?

The Archive API is backed by an archival storage backend.
A file becomes available for download when it reaches either 9 minutes of accumulation or 10 MB in size, whichever comes first.
This delay is the difference between the real-time search engine that powers the UI and the batch process that powers the API.

== See Also

* xref:index.adoc[Anypoint Monitoring]
* xref:logs.adoc[Logs in Anypoint Monitoring]
* xref:performance-and-impact.adoc#titanium[Titanium Subscription Limits]
* xref:performance-and-impact.adoc[Understanding Performance Impact, Limits, and Data Retention]
* xref:archive-api.adoc[Anypoint Monitoring Archive API]
* xref:raw-data.adoc[Raw Data in Anypoint Monitoring]
