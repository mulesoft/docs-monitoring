= Monitoring Agentic Assets

//TODO: CHECK IF API MANAGER OR ANY OTHER PRODUCT OPENS TO THIS PAGE
In US and EU Cloud instances of Anypoint Platform, navigate from Agent Visualizer to view detailed metrics on agentic assets (agent, broker, or MCP server instances).

* Agent: An autonomous software component that uses goals, context, and available tools (often via an LLM) to decide and execute actions on behalf of a user or system.
* Broker: An intelligent routing service that coordinates task delegation across specialized agents in your enterprise. Brokers originate from MuleSoft and are referenced in the agent network YAML. 
* MCP server: A service that implements the Model Context Protocol to expose tools and data to AI clients, enabling LLMs to invoke external capabilities through a standard interface.

This example shows metrics for an instance of an agent. 

[[dashboard-overview]]
//don't change order of numbers in graphic or calloutlist
image::metrics-agent.png["Metrics for an instance of an agent in Anypoint Monitoring"]

[calloutlist]
. Name of the instance
. <<properties, Properties of the instance>> that match properties in API Manager
. <<agent, Agent metrics>>
. <<lookback, Lookback period>> selected for the agent
. <<refresh, Auto-refresh>> (subscription tier requirement)
. Manual refresh option

For information about Agent Visualizer, see xref:agent-visualizer::index.adoc[]. 

== Find Metrics for Agentic Assets

TODO (FROM VISUALIZER)

//TODO: PREREQS: sub tier, permissions, instances on API Manager or Agent Visualizer? anything else?

//TODO: PROVIDE LINKS TO DOCS WITH INFO ON HOW TO OPEN THE METRICS PAGES

[[lookback]]
== Set Lookback Period for Metrics

After navigating to the metrics for an agent, broker, or MCP server instance from Agent Visualizer, get up to the last 30 days of metrics on the instance. 

* Default: *Past 1 hour*
* Maximum: *Past 30 days*

//Available periods are identical to the periods available for Anypoint Insights. 

[[properties]]
== Instance Properties

//TODO: WHERE DO THE LINKS to Runtime Target, Implementation URI, and Proxy URL go?

Agent, broker, and MCP server pages display these properties:

include::partial$include-insights-agentic-details.adoc[tag=metadata]

[[agent]]
== Agent Metrics 

Agents protected by a Flex Gateway ingress policy (xref:policies::policies-availability-by-gateway.adoc[A2A Agent Card]) produce metrics in these charts:

* *Total Received*: All requests to the agent
* *Failed Requests*: Number of requests to the agent that failed
* *Failed Request Rate*: Percentage of requests to the agent that failed 

[[broker]]
== Broker Metrics

Brokers provide these metrics in these charts:

* *Requests Received*:
** *Total Received*: Number of requests to the broker
** *Failed Requests*: Number of requests to the broker that failed
** *Failed Request Rate*: Percentage of requests to the broker that failed 

////
NOT FOR GA
* *Calls Sent*:
* *A2A Calls Sent*: Number of requests the broker sent to agents
* *MCP Calls Sent*: Number of requests the broker sent to MCP servers
////

[[mcp-server]]
== MCP Server Metrics

MCP servers protected by a Flex Gateway ingress policy (xref:policies::policies-availability-by-gateway.adoc[MCP Support]) produce metrics in these charts:

* *Total Received*: All requests to the agent
* *Failed Requests*: Number of requests to the agent that failed
* *Failed Request Rate*: Percentage of requests to the agent that failed 

The MCP server's Flex Gateway ingress supports request counts. The charts reflect calls to the MCP server through that ingress.

[[refresh]]
== Refreshing Metrics

//SUBSCRIPTION LEVELS APPLY TO AUTO-REFRESH

Refresh metrics in either of these ways:

* Manual refresh is available to all subscription levels.
* Auto refresh is available to Advanced and Titanium subscriptions.
+
Refresh cycles range from every 5 minutes to one time per day. 
